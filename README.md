# sandbox-develop-test

Throwaway public repo to smoke-test `chainctl develop` against the priya-cg
session plane (anonymous public-repo checkout). The `.chainguard/ci.yaml`
defines a `develop` job whose environment (Go + git) the resident shell opens in.
